#ifndef _CONSTS_H_
#define _CONSTS_H_

// Window
const static char WINDOW_NAME[] = "C OpenGL graphic engine";
const static float SCREEN_WIDTH = 1000.0;
const static float SCREEN_HEIGHT = 800.0;

// Input
const static float CAMERA_SPEED = 0.1;
const static float MOUSE_SENSITIITY = 0.03;

// Terrrain
const static float TERRAIN_MAX_HEIGHT = 100;
const static unsigned int TILE_SIZE = 400;
const static unsigned int TILE_MESH_SIZE = 50;

#endif